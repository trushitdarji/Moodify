# Setup

1. Install Git
2. Install Node.js (LTS)
3. Git Clone
4. npm install
5. Create .env
6. MongoDB URI
7. Redis Credentials
8. ImageKit Key
9. npm run dev

If `mongodb+srv://` fails:
→ Use standard `mongodb://` URI.